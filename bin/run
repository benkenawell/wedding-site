#!/usr/bin/env bash

# load unencrypted file at runtime
sops exec-file ./data/wedding.json \
  'podman run --rm \
  -p 3000:80 \
  -v ./srv:/srv/ \
  -v ./conf:/etc/caddy \
  -v {}:/etc/weddings/wedding.json \
  --name wedding-site \
  docker.io/library/caddy'
